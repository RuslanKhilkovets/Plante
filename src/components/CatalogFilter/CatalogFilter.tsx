import React from 'react';
import Sortings from '../Sortings/Sortings';

const CatalogFilter = () => {
    return (
        <div>
            <Sortings />
        </div>
    );
};

export default CatalogFilter;